#! This file, is a placeholder and sample for the actual `.env.development` file.
#! The actual file should be placed in the root of the project.

# Environment
ENV=testing

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/<test_db_name>

# API
JWT_ALGORITHM=***
JWT_KEYS_PASSPHRASE=***
JWT_PRIVATE_KEY_PATH=***
JWT_PUBLIC_KEY_PATH=***
JWT_LIFETIME_MINUTES=***
JWT_REFRESH_TOKEN_LIFETIME_DAYS=***
ORIGINS=["127.0.0.1:3000", "localhost:3000"]
